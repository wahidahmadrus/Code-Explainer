# Backend

Express API for the Programming Practice Assistant MVP.

Current test routes:

- `GET /api/health`
- `POST /api/ai/explain`
- `POST /api/ai/generate`

AI, Supabase, authentication, and admin role checks will be added later.
